spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/api_mulinari
    username: root
    password: root
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      dll-auto: update